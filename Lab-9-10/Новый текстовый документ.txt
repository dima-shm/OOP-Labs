mylist1.reverse(); // Меняет порядок элементов в списке контейнера(реверсирует список)

	list<int> mylist4;
	mylist4.push_back(11);
	mylist4.push_back(12);
	mylist4.push_back(15);
	mylist4.push_back(16);
	list<int> mylist5;
	mylist5.push_back(13);
	mylist5.push_back(19);
	
	mylist4.merge(mylist5);/*Объединяет упорядоченный список с другим упорядоченным списком. 
						   Результат также упорядочивается. 
						   После объединения список, содержащийся в ob, остается пустым*/

	mylist1.remove_if((mylist1.begin(), mylist1.end(), bind(isEquals(), placeholders::_1, 50)));//Удаляет элементы, для которых унарный предикат равен значению true
	
	copy(mylist1.begin(), mylist1.end(), ostream_iterator<Product>(cout, " ")); //копирует последовательность в выходной поток

	//Вектор - контейнер с произвольным доступом
	//Лист   - это динамический массив с последовательным доступом. 
	
	//Map    - это контейнер с парами ключ -> значение 
	//Set    - это массив исключающий дубликаты